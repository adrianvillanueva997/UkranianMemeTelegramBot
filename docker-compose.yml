version: '3'
services:
  bot:
    restart: unless-stopped
    image: ukranian_bot
    container_name: telegram_ukranian
    networks: 
      - bots
      - backend
networks: 
  bots:
    external: true
  backend:
    external: true


